# API

Run the Flask backend and open `/docs` for the generated API reference. The primary dashboard uses health, sensor, device, forecast, weather, camera, alert, risk, and incident-report endpoints.
